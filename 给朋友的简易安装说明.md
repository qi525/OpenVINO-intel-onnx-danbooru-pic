# 简易安装说明

> 适合直接发给朋友，简单版。

---

## 1. 安装 Python

先安装 Python 3.10 / 3.11。

安装时勾选：

- Add Python to PATH

确认安装成功：

```powershell
python --version
```

---

## 2. 进入项目目录

```powershell
cd C:\你的项目目录\danbooru-intel-onnx
```

---

## 3. 创建虚拟环境（推荐）

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
```

---

## 4. 安装依赖

```powershell
python -m pip install --upgrade pip
pip install numpy pandas opencv-python pillow tqdm
pip install onnx
```

如果电脑有 NVIDIA 显卡：

```powershell
pip install onnxruntime-gpu
```

如果没有 NVIDIA 显卡：

```powershell
pip install onnxruntime
```

---

## 5. 检查是否能导入

```powershell
python -c "import onnxruntime as ort; print(ort.get_available_providers())"
```

如果看到 `CPUExecutionProvider`，说明能跑。  
如果看到 `CUDAExecutionProvider`，说明支持 CUDA。  
如果报错，重新装依赖。

---

## 6. 把路径改对

打开脚本：

```python
latest_model_path = r"C:\danbooru-intel-onnx\最新模型\model_optimized.onnx"
latest_tag_mapping_path = r"C:\danbooru-intel-onnx\最新模型\tag_mapping.json"
labels_file_path = r"C:\danbooru-intel-onnx\tags.csv"
```

把它们改成你电脑里真实对应文件的路径。

另外也要确认 `image_paths.txt` 里写的是图片目录。

---

## 7. 运行脚本

```powershell
python main-auto-sort-v4-txt-for-cuda.py
```

---

## 8. 脚本会问什么

运行后，脚本一般会问：

- 是否确认处理这些目录
- 输入预测门限，例如 `0.5`
- 是否生成 txt 到图片同目录（yes/no）

---

## 9. 默认输出

默认会生成 CSV，表头是：

```csv
路径,tag
```

不生成 txt 的情况下，脚本只输出 CSV。

如果输入 `yes`，才会在图片同目录生成 `.txt` 文件。

---

## 10. 常见问题

### 1）找不到 onnxruntime

```powershell
pip install onnxruntime
```

### 2）模型路径错误

把 `latest_model_path` 和 `labels_file_path` 改成自己的真实路径。

### 3）没 N 卡

不用担心，脚本会自动退到 CPU。

---

## 11. 最简版命令

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install --upgrade pip
pip install numpy pandas opencv-python pillow tqdm onnx onnxruntime
python main-auto-sort-v4-txt-for-cuda.py
```

如果有人有 N 卡，可以把 `onnxruntime` 换成：

```powershell
pip install onnxruntime-gpu
```

---

如果还不懂，直接把项目文件夹发给我，我可以继续帮你把里面的路径改成“更通用的版本”，这样朋友复制过去就能直接跑。
