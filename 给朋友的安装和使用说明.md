# Danbooru 图片标签批量处理脚本安装与使用说明

本文档适合发给朋友、同事或其他使用者，帮助他们在 Windows 系统上快速安装环境并运行这个项目。

> 目标：批量处理图片，使用 ONNX 模型进行标签推理，并默认生成 CSV 结果；用户可选择是否在图片同目录生成 TXT 标签文件。

---

## 1. 先说结论

这个脚本支持两种运行方式：

1. 有 NVIDIA 显卡 + CUDA：优先走 CUDA，加速更快。
2. 没有 NVIDIA 显卡：自动退回到 CPU，仍然可以运行。

如果朋友没有 N 卡，也不用担心，脚本本身已经写了 CPU 回退逻辑。

不过前提是：

- Python 环境正确安装
- 必需依赖已安装
- 模型文件和标签文件路径正确
- 图片目录路径正确

---

## 2. 需要准备的东西

### 2.1 电脑要求

- Windows 10 / 11
- Python 3.12 或 3.13
- 40系用3.12，50系用3.13
- 至少 8GB 内存，建议 16GB 以上
- 图片存放目录最好不要太大，避免一次处理过多图片

### 2.2 必备文件

在项目目录里，至少要确认这些文件存在：

- `main-auto-sort-v4-txt-for-cuda.py`
- `image_paths.txt`
- `tags.csv`
- `最新模型/model_optimized.onnx`
- `最新模型/tag_mapping.json`

如果这些文件缺失，脚本无法正常运行。

---

## 3. 安装 Python

### 3.1 下载 Python

去官网下载：

- https://www.python.org/downloads/windows/

推荐安装：

- Python 3.12 或 3.13
- 勾选：Add Python to PATH

### 3.2 验证是否安装成功

打开命令行（PowerShell 或 cmd），输入：

```powershell
python --version
```

如果正常输出 Python 版本号，说明安装成功。

如果报错“python not recognized”，说明还没配置好 PATH；需要重新安装并勾选 Add Python to PATH，或者手动添加到环境变量。

---

## 4. 创建虚拟环境（推荐）

建议不要直接装到系统 Python，最好单独创建虚拟环境。

在项目根目录执行：

```powershell
cd C:\你的项目目录\danbooru-intel-onnx
python -m venv .venv
```

激活虚拟环境：

```powershell
.\.venv\Scripts\Activate.ps1
```

如果 PowerShell 拒绝执行脚本，可以先执行：

```powershell
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
```

然后再激活。

---

## 5. 安装依赖

先升级 pip：

```powershell
python -m pip install --upgrade pip
```

然后安装必要依赖：

```powershell
pip install numpy pandas opencv-python pillow tqdm
pip install onnx
```

### 如果电脑有 NVIDIA 显卡

推荐安装 CUDA 版本 ONNX Runtime：

```powershell
pip install onnxruntime-gpu
```

### 如果电脑没有 NVIDIA 显卡

直接安装 CPU 版本：

```powershell
pip install onnxruntime
```

### 额外说明

某些机器可能还需要：

```powershell
pip install pywin32
```

不过大多数情况不一定需要，只有在自动打开文件、打开日志/Excel 时会用到。

---

## 6. 检查依赖是否安装成功

运行下面这段命令：

```powershell
python -c "import onnxruntime as ort; print(ort.get_available_providers())"
```

### 结果说明

- 如果看到 `['TensorrtExecutionProvider', 'CUDAExecutionProvider', 'CPUExecutionProvider']` 这种输出，说明 CUDA 可用。
- 如果只看到 `['CPUExecutionProvider']`，说明当前是 CPU 模式，脚本仍然可以跑。
- 如果报错 `No module named onnxruntime`，说明没有安装成功。

也可以测试：

```powershell
python -c "import cv2, numpy, pandas, PIL, tqdm; print('deps ok')"
```

---

## 7. 确认项目中的路径

打开脚本：

```python
latest_model_path = r"C:\danbooru-intel-onnx\最新模型\model_optimized.onnx"
latest_tag_mapping_path = r"C:\danbooru-intel-onnx\最新模型\tag_mapping.json"
labels_file_path = r"C:\danbooru-intel-onnx\tags.csv"
label_column_name = 'tag'
```

这些路径必须改成“你电脑上的真实路径”。

### 最稳妥的办法

直接将项目整个文件夹复制到朋友的电脑上，然后把这些路径改成该项目的本地相对路径，或者改成同一目录下的绝对路径。

例如：

```python
script_dir = os.path.dirname(os.path.abspath(__file__))
latest_model_path = os.path.join(script_dir, "最新模型", "model_optimized.onnx")
latest_tag_mapping_path = os.path.join(script_dir, "最新模型", "tag_mapping.json")
labels_file_path = os.path.join(script_dir, "tags.csv")
```

这样更不容易出问题，因为不依赖固定盘符。

---

## 8. 修改图片目录文件

脚本会读取 `image_paths.txt` 文件。

这个文件里，每一行写一个图片目录路径，例如：

```text
D:\images\anime_batch_1
E:\sorted\folder2
C:\Users\xxx\Pictures\test_images
```

### 重要

- 每行一个目录
- 不能空白
- 注释行可以用 `#`
- 目录必须是真实存在的图片文件夹

如果 `image_paths.txt` 为空或路径错误，脚本会提示没有有效目录。

---

## 9. 运行脚本

在激活虚拟环境后，进入项目目录：

```powershell
cd C:\你的项目目录\danbooru-intel-onnx
.\.venv\Scripts\Activate.ps1
python main-auto-sort-v4-txt-for-cuda.py
```

脚本启动后会依次输出：

- CUDA / CPU 检测
- ONNX Runtime provider 检测
- 模型路径检查
- 标签加载
- 图片目录确认
- 预测阈值询问
- 是否生成 TXT 文件询问

---

## 10. 脚本会询问什么

运行时常见交互：

### 10.1 是否确认处理目录

脚本会列出要处理的目录，询问：

```text
确认处理？(y/n)
```

输入：

- `y`：继续
- `n`：取消

### 10.2 输入预测门限

例如：

```text
请输入预测门限 (例如: 0.5, 范围 0.0-1.0):
```

建议默认值：

- 0.5
- 如果标签太多或太杂，可以调小
- 如果想更保守，可调高一些

### 10.3 是否生成 TXT

脚本会询问：

```text
是否在对应图片文件夹中生成 txt 标签文件？输入 yes/no（默认 no）
```

- 输入 `yes`：图片同目录会生成 `.txt` 文件
- 输入 `no`：不生成 TXT，只生成默认 CSV

---

## 11. 脚本默认输出什么

默认会输出 CSV，且 CSV 的表头固定为两列：

```csv
路径,tag
```

例如：

```csv
路径,tag
D:\images\a.jpg,"1girl, smile"
D:\images\b.jpg,solo
```

### 说明

- CSV 是默认输出
- TXT 是可选输出
- TXT 中通常只保存标签文本，不会保存权重/置信度

---

## 12. 脚本中常见的几个关键词

### `label_column_name = 'tag'`

表示：CSV 里真正的标签列叫 `tag`。

如果 CSV 头不是 `tag`，就要改成对应列名。

### `latest_model_path`

表示：模型文件位置。

### `latest_tag_mapping_path`

表示：模型标签映射 JSON 文件位置。

### `image_paths.txt`

表示：要处理的图片目录列表。

---

## 13. 常见问题及解决方法

### 13.1 `No module named onnxruntime`

解决：

```powershell
pip install onnxruntime
```

或者：

```powershell
pip install onnxruntime-gpu
```

### 13.2 `CUDAExecutionProvider` 没有出现

这是正常的，不代表脚本不能跑。

表示当前机器没有 NVIDIA CUDA 环境，脚本会自动使用 CPU。

### 13.3 模型文件路径找不到

检查：

- 文件名是否正确
- 路径是否存在
- 是否用了错误的盘符或中文路径
- 是否路径中有空格或特殊字符

### 13.4 图片文件夹没识别到

检查：

- `image_paths.txt` 是否填写正确
- 目录是否存在
- 目录里是否确实有 `.jpg`、`.jpeg`、`.png` 等图片

### 13.5 CSV 结果为空

检查：

- 模型是否能加载
- 标签文件是否读取成功
- 预测是否正常执行

---

## 14. 推荐启动顺序（最稳妥）

1. 安装 Python
2. 创建 venv
3. 安装依赖
4. 确认 `onnxruntime` 可用
5. 检查模型和标签文件是否存在
6. 修改 `image_paths.txt`
7. 修改脚本中的模型路径和标签路径
8. 运行脚本
9. 观察输出日志，确认是否正常

---

## 15. 最后建议

如果朋友是第一次用，建议直接使用以下步骤：

```powershell
cd 你的项目目录
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install --upgrade pip
pip install numpy pandas opencv-python pillow tqdm onnx
pip install onnxruntime
python main-auto-sort-v4-txt-for-cuda.py
```

如果是 NVIDIA 显卡用户，可以把最后一行换成：

```powershell
pip install onnxruntime-gpu
```

---

## 16. 结尾

如果按以上步骤执行，通常就能正常运行这个脚本。

如果你朋友装完后还遇到问题，可以把：

- Python 版本
- `onnxruntime` 版本
- `ort.get_available_providers()` 的输出
- 脚本报错日志

发给你一起看，排查会更快。

---

如果你愿意，我也可以继续给你一个“更适合直接发给朋友的精简版说明”，或者直接把这个文档改成更短、更容易复制粘贴的版本。
